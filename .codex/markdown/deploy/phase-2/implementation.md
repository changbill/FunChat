# Phase 2 구현

- 영역: deploy. 브랜치: `feature/2-livekit-deploy-env`.
- 롤링 Compose 공통 앱 템플릿에 LiveKit 환경변수 4개를 필수값 구문으로 추가했다.
- README에 Jenkins 환경 파일 설정 방법, SPEC에 전달 및 오류 계약을 기록했다.
- backend 설정이나 인프라 Compose 변경은 필요하지 않았다.
